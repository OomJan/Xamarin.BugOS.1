<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BugOS.Views.ItemsPage"
              Title="{Binding Title}"
             x:Name="BrowseItemsPage">
    <StackLayout>
        <Button Text="Do Test" Command="{Binding DoStuffCommand}"/>
        <Label Text="{Binding Something.SomeProperty}"/>
        <Label Text="{Binding Something.AnotherProperty}"/>
        <Label Text="{Binding Something.ButThisProperty}"/>
    </StackLayout>
</ContentPage>